<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= surahs.number %>. <%= surahs.transliteration_en %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <!-- to use badge from w3 -->
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link rel="shortcut icon" href="https://www.freeiconspng.com/thumbs/quran-icon-png/quran-icon-png-7.png" type="image/x-icon">
</head>

<body>
    <% if(locals.si) { %>
    <div class="card">
        <!-- pagination starts -->
        <% if(surahs.number>1) {%>
        <nav aria-label="Page navigation example">
            <ul class="pagination">
                <li class="page-item">
                    <a class="page-link" href="/<%= surahs.number - 1 %>">Previous</a>
                </li>
            </ul>
        </nav>
        <% } %>

        <button type="button" class="btn btn-primary btn-lg" style="font-size: x-large;">
            <%= surahs.number %>. <%= surahs.transliteration_en %> (<%= surahs.translation_en %>) - <%= surahs.name %>
            <a href="/" style="float: right;text-decoration: none;">«Home</a>
        </button>

        <ul class="list-group list-group-flush">
            <% for(let i=0;i<si.length;i++) { %>
            <li class="legend" style="font-size: 150%;">
                <div class="card">
                    <span class="w3-badge w3-green"><%= si[i].verse_number %></span>
                    <div class="card-body"
                        style="text-align: right; font-size: 200%; font-family:Droid Arabic Naskh, san-serif;">
                        <%= si[i].text %>
                    </div><span class="card-title"><%= si[i].translation %></span>
                    <p class="card-subtitle mb-2 text-muted">- Sahih International</p><br>
                    <span class="card-title"><%= ya[i].text %></span>
                    <p class="card-subtitle mb-2 text-muted">- Abdullah Yousuf Ali</p><br>
                    <span class="card-title"><%= bn[i].text %></span>
                    <p class="card-subtitle mb-2 text-muted">- Bengali Translation</p>
                </div>
            </li>
            <% } %>
        </ul>

        <!-- pagination starts -->
        <% if(surahs.number<114) { %>
        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-end">
                <li class="page-item">
                    <a class="page-link" href="<%= surahs.number + 1 %>">Next</a>
                </li>
            </ul>
        </nav>
        <% } %>
    </div>
    <% } %>

</body>

</html>